const arg1 = process.argv[2];
const arg2 = process.argv[3];
console.log(`${arg1} is ${arg2}`);
// This code retrieves the first two command line arguments passed to the script and prints them in a formatted string.